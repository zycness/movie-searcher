import React from "react";
import styles from "../styles/home.module.css";

const Home = () => {
  return (
    <div className={styles.homeContainer}>
      <h2 className={styles.homeTitle}>Peliculas recomendadas</h2>
    </div>
  );
};

export default Home;
